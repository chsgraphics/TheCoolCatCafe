.header.sticky.spin#navbar
  img.logo(src='/images/logo.png')#logo
  .purple-block
    p.title The Cool Cat CafÉ
  .cream-block
    .link-container
    each link in links
      - let [name, url] = link;
      a.nav-links(class={'is-active': page === name}, href=url) #{name}
        
